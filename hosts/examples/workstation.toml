# Copy to ../<hostname>.toml or run: rig init --role workstation
name = "my-laptop"
role = "workstation"
schema_version = 1
# os / shell omitted → detected at apply time
# user = "you"
# No VPN/LAN required for a workstation laptop
