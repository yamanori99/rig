# Product Cursor User templates live in templates/cursor/User/.
# Optional personal overrides (gitignored): overlay/cursor/User/{settings,keybindings}.json
# Prefer overlay when present; otherwise templates are linked on apply (features.cursor).
