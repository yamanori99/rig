/target
**/target
.DS_Store
*.swp

# Real machine inventory lives in ~/.rig-hosts — never commit it here
/hosts/*.toml
!/hosts/examples/

# Personal overlays
/overlay/**
!/overlay/.gitkeep

# Local state
.rig-state.json

# testenv secrets / generated (defense in depth)
testenv/**/.generated/
testenv/**/mounted-keys/**
!testenv/**/mounted-keys/.gitkeep

# Cursor User JSON under templates (live copies; seeds are *.example)
/templates/cursor/User/settings.json
/templates/cursor/User/keybindings.json
