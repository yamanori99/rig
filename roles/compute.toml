description = "Headless compute node — CLI runtimes, no GUI casks"
packages = ["common", "compute"]
default_shell = "bash"

[features]
gui = false
cursor = false
remote_login = true
tailscale = false
thunderbolt = true
stay_awake = true
